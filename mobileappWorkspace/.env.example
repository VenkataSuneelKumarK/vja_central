# Read by app.config.ts — set per environment (local/staging/production).
# EAS build profiles should set these as build-time env vars, not commit them.
API_BASE_URL=http://localhost:4000/api
APP_ENV=development
